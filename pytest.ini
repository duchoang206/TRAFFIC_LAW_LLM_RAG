[pytest]
testpaths = tests
python_files = test_*.py
markers =
    requires_qdrant: test needs a reachable Qdrant instance (auto-skipped in CI)
    slow: test may take >30s (e.g. retrieval regression on full eval set)
addopts = -v --tb=short
